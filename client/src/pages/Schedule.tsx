/* VID-GEN schedule: dark calendar board, platform-coded posts, and mobile-friendly upcoming queue. */
import { CalendarDays, ChevronLeft, ChevronRight, Clock3, Instagram, Plus, Twitter, Youtube } from "lucide-react";
import { PlatformShell } from "@/components/PlatformShell";

const days = ["MON 12", "TUE 13", "WED 14", "THU 15", "FRI 16", "SAT 17", "SUN 18"];
const events = [{ day: 0, time: "09:30", title: "3 habits that changed my mornings", tone: "red", Icon: Instagram }, { day: 1, time: "12:00", title: "Behind the scenes: a better workflow", tone: "blue", Icon: Youtube }, { day: 3, time: "08:00", title: "One small idea worth saving", tone: "yellow", Icon: Twitter }, { day: 5, time: "10:45", title: "Make room for the good stuff", tone: "green", Icon: Instagram }];
export default function Schedule() { return <PlatformShell title="Schedule"><div className="page-heading schedule-heading"><div><span className="eyebrow"><span className="eyebrow-line" /> YOUR PUBLISHING RHYTHM</span><h2>Keep the week<br /><em>moving forward.</em></h2><p>See what is ready, what is next, and where your ideas are about to land.</p></div><button className="button button-yellow"><Plus size={16} /> Schedule a post</button></div><section className="calendar-panel app-panel"><div className="calendar-toolbar"><button className="icon-btn"><ChevronLeft size={17} /></button><h3>August 2026</h3><button className="icon-btn"><ChevronRight size={17} /></button><span className="calendar-spacer" /><button className="calendar-today">Today</button><span className="status-badge green"><i /> 12 posts ready</span></div><div className="calendar-grid"><div className="calendar-corner" />{days.map(day => <div className="calendar-day" key={day}>{day}</div>)}{["09:00", "12:00", "15:00", "18:00"].map(time => <div className="calendar-row" key={time}><span className="calendar-time">{time}</span>{days.map((_, day) => { const event = events.find(item => item.day === day && item.time === time); return <div className="calendar-cell" key={`${time}-${day}`}>{event && <div className={`calendar-event ${event.tone}`}><event.Icon size={12} /><span>{event.title}</span></div>}</div>; })}</div>)}</div></section><section className="upcoming-panel app-panel"><div className="panel-heading"><div><span className="mini-label">UP NEXT</span><h3>The queue is looking good.</h3></div><button className="panel-link">Open calendar <CalendarDays size={14} /></button></div><div className="upcoming-list">{events.map(event => <div className="upcoming-row" key={event.title}><span className={`upcoming-icon ${event.tone}`}><event.Icon size={16} /></span><span><b>{event.title}</b><small><Clock3 size={12} /> {days[event.day]} · {event.time}</small></span><span className="upcoming-action">Edit <ChevronRight size={15} /></span></div>)}</div></section></PlatformShell>; }

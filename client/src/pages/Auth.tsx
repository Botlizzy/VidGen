/* Midnight Studio auth: calm, high-contrast form surface with the same signal-blue action language as the landing page. */
import { FormEvent, useState } from "react";
import { ArrowLeft, ArrowUpRight, Eye, EyeOff, LockKeyhole, Mail, UserRound } from "lucide-react";
import { Link, useLocation } from "wouter";

const logo = "/manus-storage/vid-gen-mark_b4fd1632.png";

export default function Auth({ mode }: { mode: "login" | "signup" }) {
  const isSignup = mode === "signup";
  const [, navigate] = useLocation();
  const [showPassword, setShowPassword] = useState(false);
  const [submitted, setSubmitted] = useState(false);
  const onSubmit = (e: FormEvent) => { e.preventDefault(); setSubmitted(true); };
  return <div className="auth-shell"><div className="auth-left"><Link href="/" className="auth-back"><ArrowLeft size={16} /> Back to home</Link><div className="auth-brand"><img src={logo} alt="" /><span className="brand-name"><span className="brand-vid">VID</span><span className="brand-cut">·</span><span className="brand-gen">GEN</span></span></div><div className="auth-pitch"><span className="mono">{isSignup ? "YOUR STUDIO AWAITS" : "WELCOME BACK, CREATOR"}</span><h1>{isSignup ? <>Your next<br /><em>run starts here.</em></> : <>Good to see<br /><em>you again.</em></>}</h1><p>{isSignup ? "Turn the ideas in your head into content your audience can feel — without adding another tab to your life." : "Pick up where you left off and keep the content moving."}</p></div><div className="auth-signal mono"><span className="live-dot" /> SYSTEMS ONLINE <span>VID-GEN / 01</span></div></div><div className="auth-right"><div className="auth-form-wrap"><div className="auth-utility mono"><span><span className="live-dot" /> CREATOR PIPELINE</span><span>SECURE / 01</span></div><div className="mobile-auth-brand"><img src={logo} alt="" /><span className="brand-name"><span className="brand-vid">VID</span><span className="brand-cut">·</span><span className="brand-gen">GEN</span></span></div><div className="auth-heading"><span className="mono">{isSignup ? "OPEN YOUR CREATOR DESK" : "RETURN TO YOUR CREATOR DESK"}</span><h2>{isSignup ? "Make room for the good stuff." : "Your studio, ready."}</h2><p>{isSignup ? "A better content rhythm is one focused workspace away." : "Pick up your publishing desk where you left it."}</p></div>{submitted ? <div className="submitted-state"><div className="success-mark"><ArrowUpRight size={22} /></div><h3>{isSignup ? "Your studio is ready to go." : "You’re back in."}</h3><p>This demo form is connected visually. Hook it up to your auth provider when you’re ready.</p><button className="primary-btn" onClick={() => navigate("/")}>Return home <ArrowUpRight size={16} /></button></div> : <form onSubmit={onSubmit} className="auth-form">{isSignup && <label><span>Name</span><div className="input-wrap"><UserRound size={17} /><input required placeholder="Your name" /></div></label>}<label><span>Email</span><div className="input-wrap"><Mail size={17} /><input type="email" required placeholder="you@creator.com" /></div></label><label><span>Password</span><div className="input-wrap"><LockKeyhole size={17} /><input type={showPassword ? "text" : "password"} required minLength={6} placeholder="At least 6 characters" /><button type="button" className="password-toggle" aria-label={showPassword ? "Hide password" : "Show password"} onClick={() => setShowPassword(!showPassword)}>{showPassword ? <EyeOff size={16} /> : <Eye size={16} />}</button></div></label>{isSignup && <label className="check-label"><input type="checkbox" required /><span>I agree to the <a href="#terms">terms</a> and <a href="#privacy">privacy policy</a>.</span></label>}<button type="submit" className="primary-btn full-btn">{isSignup ? "Create account" : "Sign in"} <ArrowUpRight size={17} /></button></form>}<div className="auth-switch">{isSignup ? "Already have an account?" : "New to VID-GEN?"} <Link href={isSignup ? "/login" : "/signup"}>{isSignup ? "Sign in" : "Create an account"}</Link></div></div><div className="auth-footer mono">SECURE CREATOR ACCESS <span>●</span></div></div></div>;
}
